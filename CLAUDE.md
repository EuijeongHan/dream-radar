# CLAUDE.md — Radar 저장소 작업 규칙

> 이 파일을 먼저 읽고 시작하세요. 아래 규칙은 협상 대상이 아닙니다.

## 1. 문서를 읽는 순서 ★

`docs/`의 파일들은 **끝에 붙은 번호가 클수록 최신**입니다. 번호 없는 파일이 가장 오래된 버전입니다.

**같은 이름의 파일이 여러 개면 최고 번호 하나만 읽으세요.** 구버전은 이력 보존용이며 내용이 다릅니다. 구버전을 읽고 구현하면 확정된 제약이 빠집니다.

읽는 순서:

1. `docs/radar_기획안_1.md` — **본 기획안 (최신)**. `radar_기획안.md`는 구버전이니 읽지 마세요
2. `docs/radar_기획안_델타_v1.1.md` — 위 문서의 정정·추가분. **충돌 시 델타가 우선**합니다
3. `data/profile.papers.yaml` (영어, 운영용) / `data/profile.papers.ko.yaml` (평가 대조군)
4. `data/profile.jobs_1.yaml` — **최신**. `profile.jobs.yaml`은 구버전

구현은 기획안 §8 마일스톤 순서대로. **각 마일스톤의 DoD를 만족하기 전에 다음으로 넘어가지 마세요.**

## 2. 파일명 번호 규칙 ★

**기존 파일을 덮어쓰지 않습니다.** 파일을 새로 만들거나 고칠 때는 확장자 바로 앞에 `_N`을 붙여 새 파일로 저장합니다.

```
radar_기획안.md  →  radar_기획안_1.md  →  radar_기획안_2.md
profile.jobs.yaml → profile.jobs_1.yaml → profile.jobs_2.yaml
```

- 저장 전에 폴더를 읽고 해당 이름의 **최대 번호를 확인한 뒤 +1** 로 씁니다
- 번호는 **항상 맨 끝**입니다. 이름에 `v1.1` 같은 게 이미 있어도 규칙을 바꾸지 않습니다 (`..._v1.1_1.md`)
- 기존 파일은 이동·삭제·수정하지 않습니다
- 예외: `CLAUDE.md`, `README.md`, `.gitignore`, 소스코드(`src/`, `tests/`, `eval/`) — 이들은 git이 이력을 관리하므로 그냥 덮어씁니다

## 3. 절대 하지 말 것

이 저장소는 **public**입니다. 아래는 법적·약관 제약이며 어떤 우회도 하지 않습니다.

| # | 금지 | 근거 |
|:--:|:--|:--|
| 1 | `data/profile.jobs*.yaml`, `data/jobs/`를 커밋 | 구직 조건 노출. `.gitignore` 등록됨 |
| 2 | API 키를 코드·로그·원장·커밋에 남기기 | 사람인 약관: access-key 공개·공유 금지 |
| 3 | 채용공고를 공개 게시 (사이트·블로그·노션 웹게시) | 사람인 약관: 제휴 관계 오인 금지. **무료여도 금지** |
| 4 | arXiv PDF·전문을 다운로드·저장·서빙 | 대부분 재배포 불가 라이선스. **초록만** 사용 (초록은 CC0) |
| 5 | 잡코리아·알바몬·원티드·리멤버·링크드인·사람인 웹페이지 스크래핑 | DB제작자 권리 침해 판례. 공식 API만 사용 |
| 6 | arXiv 요청 병렬화 | **3초 1회 + 단일 커넥션.** 권장이 아니라 요구사항. 본인 통제 머신 전체에 합산 적용 |
| 7 | 사람인 일 500회 초과 호출 | 에러코드 4 수신 시 즉시 중단·원장 기록·재시도 금지 |
| 8 | jobs 채널에 faithfulness 검증 적용 | 사람인 API가 본문을 주지 않아 대조할 원문이 없음. 필터 위반 검사로 대체 |
| 9 | 요약 검증자를 생성자와 같은 계열 모델로 설정 | 동일가족 편향. 검증이 성립하지 않음 |

`docs/_reference/`의 벤더 문서(PDF·HWP)도 커밋하지 않습니다.

## 4. 지금 상태

API 키 **미발급**. 하지만 **M0·M1·M3은 키 없이 완주 가능**합니다 (델타 §D6).

- arXiv API는 인증이 없습니다
- `bge-reranker-v2-m3`는 원래 로컬 CPU 모델입니다
- M1 평가는 Actions가 아니라 로컬 1회성 배치이므로 `bge-m3` 로컬 임베딩을 씁니다. 이건 기획안 §5.1(1)의 "러너에서 로컬 임베딩 안 돌린다" 결정과 충돌하지 않습니다 — 그건 운영 결정입니다

**키를 기다리며 멈추지 마세요.** 델타 §D6.5의 작업 순서 1~6을 먼저 끝냅니다.

## 5. 폴더 구조

```
radar/
├── CLAUDE.md              이 파일
├── docs/                  기획안·델타 (번호 규칙 적용)
│   └── _reference/        벤더 문서. gitignore
├── data/                  프로파일·원장·상태
├── src/                   기획안 §6 구조
├── eval/                  골드셋·평가 (§9)
├── site/                  Hugo (§5)
├── tests/                 pytest 게이트 (§10 + 델타 §D8)
│   └── fixtures/          API 응답 샘플 — 키 없이 테스트
└── .github/workflows/
```

## 6. 판단이 애매하면

**구현을 멈추고 질문하세요. 추측으로 채우지 마세요.**
특히 §3의 9개 항목에 걸릴 소지가 있는 작업은 반드시 확인을 받습니다.

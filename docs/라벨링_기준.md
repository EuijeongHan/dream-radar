# 골드셋 라벨링 기준

> **템플릿입니다. §3·§4·§5는 본인이 채워야 합니다.**
> 기획안 §9-4: *"라벨링 기준을 문서로 남길 것 — '왜 이걸 관련 있다고 했는가'가 없으면 재현 불가."*
>
> 라벨링을 **시작하기 전에 §3을 초안이라도 써두고**, 라벨링하면서 §5에 판정을 누적하세요.
> 다 끝내고 몰아서 쓰면 그건 기준이 아니라 사후 합리화입니다.

| | |
|:--|:--|
| 라벨러 | 한의정 (1인) |
| 대상 | `data/candidates/{date}.jsonl` 3일치 |
| 산출물 | `eval/goldset.yaml` |
| 도구 | `python -m eval.label` |
| 작성 시작 | 2026-08-12 |

---

## 1. 이 골드셋이 답하려는 질문

**"내가 그날 아침에 실제로 읽었을 논문인가."**

"좋은 논문인가"도 "인용이 많을 논문인가"도 아닙니다. 이 시스템은 개인화 랭커이고,
정답은 **이 사람이 읽고 싶어 했는가**입니다. 그래서 1인 라벨러가 한계가 아니라 정의입니다
(기획안 §11의 프레이밍과 같습니다).

판정이 흔들릴 때 돌아올 기준 문장:

> 이 논문이 내일 아침 텔레그램 5건 안에 들어왔다면, 나는 링크를 눌렀을까?

---

## 2. 절차

### 2.1 2패스 구조와 그 이유

하루 후보가 700건대인데 관련 논문은 3~7건입니다. 700건 초록을 다 읽을 수는 없습니다.
그렇다고 **랭커로 상위 N건을 뽑아 그것만 라벨링하면 평가가 무너집니다** — 평가 대상인
랭커가 정답셋의 범위를 정하게 되어, 랭커가 놓친 논문은 애초에 정답이 될 기회가 없습니다.
개선폭이 실제보다 좋게 나옵니다.

→ **전수 커버리지를 유지하고, 읽는 깊이를 2단계로 나눕니다.**

| 패스 | 보는 것 | 판정 | 기록 |
|:--:|:--|:--|:--|
| 1 | 제목 + 카테고리 | 무관 확정 / 보류 | `basis: title` |
| 2 | 초록 전문 | 관련 / 무관 | `basis: abstract` |

1패스에서 거른 것도 **정식 라벨(`relevant: false`)** 입니다. 다만 `basis`를 남겨서
나중에 "제목만 보고 놓친 게 아닌가"를 검증할 수 있게 합니다.

**1패스의 질문은 "관련 있는가"가 아니라 "초록을 읽을 가치가 있는가"입니다.**
확신이 없으면 무조건 보류(`k`)입니다. 1패스에서 잘못 버린 논문은 영원히 정답이 될 수
없지만, 잘못 보류한 논문은 2패스에서 걸러집니다. **비대칭이므로 보류 쪽으로 기울이세요.**

제목만으로 판단이 안 서는데 지금 궁금하면 **`a`로 초록을 펼쳐 그 자리에서 y/n/k 판정**할
수 있습니다. 이때 판정은 `basis: abstract`로 기록됩니다 — 초록을 읽고 내린 판정을
`basis: title`로 남기면 재검토(§2.4)의 누락률 측정이 오염되기 때문입니다.
단 `a`를 남발하면 1패스의 속도 이점이 사라지니, 기본은 여전히 `k`입니다.

### 2.2 순서를 섞는 이유

도구가 후보를 날짜별 고정 시드로 섞어서 보여줍니다. arXiv 제출 순서 그대로 라벨링하면
뒤로 갈수록 집중력이 떨어지는 효과가 **특정 카테고리에 체계적으로** 몰립니다(제출 시각과
분야에 상관이 있으므로). 섞으면 피로 효과가 편향이 아니라 잡음이 됩니다.
시드가 날짜에서 나오므로 중단 후 재개해도 순서는 같습니다.

### 2.3 명령

```bash
python -m eval.label triage  --date 2026-08-12   # 1패스
python -m eval.label review  --date 2026-08-12   # 2패스
python -m eval.label recheck --date 2026-08-12   # 1패스 누락률 측정 (표본 40건)
python -m eval.label status                      # 진행률
python -m eval.label export                      # eval/goldset.yaml 생성
```

언제든 `q`로 나가면 그 지점까지 저장됩니다. `u`는 직전 판정 취소입니다.

### 2.4 `recheck` — 1패스를 스스로 검증하는 단계 ★

1패스는 제목만 봅니다. 제목이 불친절한 논문을 놓쳤을 수 있고, **놓쳤는지 아닌지를 모르면
§6의 한계 2가 영원히 "그럴 수도 있다"로 남습니다.**

`recheck`는 1패스에서 버린 것 중 **표본 40건**을 뽑아 초록까지 읽게 합니다. 표본에서
몇 건이 뒤집히는지가 곧 1패스 누락률의 추정치이고, 이 수치는 `goldset.yaml`의
`summary.title_pass_recheck`에 기록됩니다.

- 뒤집힘 0건 → 1패스 기준이 신뢰할 만하다는 **근거**가 됩니다
- 뒤집힘이 여럿 → 1패스 기준이 너무 빡빡합니다. §3을 고치고 해당 날짜를 다시 하세요
- 안 돌리면 `sampled: 0`으로 기록됩니다. **안 한 걸 안 했다고 적는 것도 정직한 골드셋의 일부입니다**

날짜당 1회, 2패스를 끝낸 뒤에 돌리세요.

### 2.4 한 세션의 크기

**한 번에 200건을 넘기지 마세요.** 피로가 누적되면 후반부가 전부 `n`이 됩니다.
1패스 700건은 3~4세션으로 나누는 걸 권합니다.

---

## 3. 포함 기준 ★ 본인이 확정할 것

> 아래는 `data/profile.papers_1.yaml`의 관심사 15개에서 **기계적으로 옮긴 초안**입니다.
> 그대로 쓰지 마세요. 프로파일은 *검색 질의*이고 이 문서는 *정답 판정 기준*입니다.
> 둘이 같으면 "프로파일로 검색해서 프로파일과 닮은 걸 찾았다"는 동어반복이 됩니다.
>
> **각 항목에 "이런 논문이면 관련"의 구체적 조건을 본인 문장으로 덧붙이세요.**

### 3.1 핵심 (프로파일 weight 1.0)

| id | 초안 | 본인 판정 조건 |
|:--|:--|:--|
| `identity_preserving_edit` | 확산 모델 이미지 편집에서 객체 동일성 보존 | *(TODO)* |
| `text_rendering_cjk` | 생성 이미지 내 텍스트 렌더링, 특히 한글·CJK | *(TODO)* |
| `judge_bias` | LLM-as-a-Judge 신뢰성·자기선호 편향·평가자 분리 | *(TODO)* |
| `faithfulness_hallucination` | 요약 충실도, 환각 탐지, 근거 지지도 채점 | *(TODO)* |
| `rag_retrieval_eval` | RAG 검색 품질, 리랭킹, Hit@k·MRR·nDCG 평가 | *(TODO)* |

### 3.2 인접 (weight 0.9 / 0.8)

| id | 초안 | 본인 판정 조건 |
|:--|:--|:--|
| `abstention_rejection` | 근거 없을 때 거절하는 능력 | *(TODO)* |
| `multilingual_korean_retrieval` | 한국어·교차언어 검색 | *(TODO)* |
| `query_routing` | 질의 유형별 라우팅·적응형 검색 | *(TODO)* |
| `tool_grounded_numeric` | 수치 계산을 코드에 위임 | *(TODO)* |
| `efficient_inference` | 추론 경량화·로컬 서빙 | *(TODO)* |
| `agent_orchestration` | 에이전트 오케스트레이션·관측성 | *(TODO)* |
| `data_aug_imbalance` | 데이터 증강·클래스 불균형 | *(TODO)* |
| `doc_parsing` | 문서 파싱·표·수식 추출 | *(TODO)* |

### 3.3 주변 (weight 0.6)

| id | 초안 | 본인 판정 조건 |
|:--|:--|:--|
| `gen_eval_metrics` | 생성 이미지 자동 평가 지표 | *(TODO)* |
| `image_restoration` | 이미지 복원·디노이징 | *(TODO)* |

### 3.4 프로파일에 없지만 관련으로 볼 것 ★

> **여기가 이 문서의 존재 이유입니다.** 프로파일에 없는데 읽고 싶은 논문이 나오면
> 그건 프로파일의 결함이지 정답의 결함이 아닙니다. 관련으로 라벨링하고 여기 적으세요.
> M1 평가에서 "랭커가 이걸 못 잡았다"가 되고, M6 피드백 환류의 근거가 됩니다.

- *(라벨링하면서 채우세요)*

---

## 4. 제외 기준 ★ 본인이 확정할 것

`profile.papers_1.yaml`의 `exclude.soft`는 **감점**이지 배제가 아닙니다(기획안 §7-4).
골드셋에서는 이분법이므로 별도로 정해야 합니다.

| 유형 | 초안 판정 | 본인 확정 |
|:--|:--|:--|
| 순수 이론 최적화 (수렴 증명, 실증 없음) | 무관 | *(TODO)* |
| 로보틱스 제어·자율주행 | 무관 | *(TODO)* |
| 계산생물학·단백질·분자 | 무관 | *(TODO)* |
| 양자 컴퓨팅 | 무관 | *(TODO)* |
| 실험 없는 입장문·논평 | 무관 | *(TODO)* |
| **서베이** | ? — 프로파일에서 penalty 0.3으로 가장 약함. "분야 입문에 유용"이라 적혀 있음 | **확정 → §4.1** |
| 벤치마크/데이터셋 공개 논문 | ? | *(TODO)* |
| 이미 아는 내용의 점진적 개선 (delta가 작은 후속 연구) | ? | *(TODO)* |

**서베이 판정을 먼저 정하세요.** 하루에 여러 건 나오고, 판정이 흔들리면 라벨 일관성이
가장 크게 깨지는 항목입니다.

### 4.1 서베이 판정 — 확정 (2026-08-16, 라벨링 시작 전)

**규칙: 형식이 아니라 주제로 판정하되, 읽기 문턱을 한 단계 올린다.**

1. 서베이라는 형식을 지우고 묻는다 — 이 주제의 일반 논문이었어도 무관인가?
   무관이면 **무관** (형식과 관계없이).
2. 주제가 관련이면, 그 주제가 **weight 1.0·0.9 관심사의 정면**인지 본다.
   - 정면 (그 관심사가 서베이의 주된 대상) → **관련**
   - 0.8 이하 관심사이거나 여러 관심사에 비스듬히 걸침 → **무관**. 애매하면 `m` 메모
3. 제목에 "Survey"가 없어도 초록에서 서베이로 드러나면 같은 기준.

**왜 이 규칙인가**

- §1의 기준 문장에 정직하다. 지금 M1·M2를 직접 만드는 중이라 `rag_retrieval_eval`·
  `faithfulness_hallucination`·`judge_bias` 서베이는 실제로 연다. `image_restoration`
  (0.6, 과거 프로젝트) 서베이는 열지 않는다. 1.0/0.9 vs 0.8 경계는 프로파일 작성 때
  이미 그어둔 선("직접 다뤘거나 곧 필요한" vs "인접")이라 새 자의적 기준이 아니다.
- **전부-무관으로 라벨링하면 exclude.soft의 `penalty: 0.3`을 영영 검증할 수 없다.**
  골드셋이 "서베이 = 무조건 오답"이라 말하면 M1 평가는 페널티를 키울수록 점수가
  좋아지고, M6 가중치 갱신이 프로파일의 "입문에 유용" 설계와 모순된 방향으로 간다.
  정직하게 라벨링해야 0.3이 맞는 값인지 실측된다.
- **1패스에서는 이 규칙을 적용하려 하지 말 것.** 제목만으로 "정면" 여부를 가리기
  어렵다. 서베이 제목이 코어 주제면 `k`(보류), 명백히 무관한 주제면 `n`.

이 규칙과 어긋나는 사례가 나오면 §5에 기록하고 이 절을 갱신한다.

---

## 5. 경계 사례 판정 기록 ★ 라벨링하면서 누적

> 애매했던 논문과 **그때 어떻게 판정했는지**를 남깁니다. 이게 있어야 다음 사람이
> (또는 3개월 뒤의 본인이) 같은 기준으로 라벨링할 수 있습니다.
> 도구에서 `m`을 누르면 메모가 골드셋에 함께 저장됩니다.

| 날짜 | item_id | 제목(축약) | 왜 애매했나 | 판정 | 근거 |
|:--|:--|:--|:--|:--:|:--|
| | | | | | |

### 판정에서 도출된 규칙

> 위 표가 쌓이면 반복되는 패턴이 보입니다. 그때 여기에 규칙으로 승격시키세요.
> 예: *"의료 영상 도메인의 RAG 논문은 방법이 도메인 특화면 무관, 검색 구조가 일반적이면 관련"*

- *(TODO)*

---

## 6. 알려진 한계 ★ 숨기지 말 것

포트폴리오에 이 골드셋의 수치를 쓸 때 **함께 명시해야 하는 항목**입니다.
한계를 적어두는 게 수치의 신뢰도를 낮추는 게 아니라 높입니다.

| # | 한계 | 영향 | 완화 |
|:--:|:--|:--|:--|
| 1 | **라벨러 1인** | 라벨러 간 일치도(IAA)를 계산할 수 없음 | 개인화 시스템이므로 개인 기준이 정답이라는 프레이밍이 정직함 (기획안 §11) |
| 6 | **제목·초록을 Claude가 기계번역해 병기함** (라벨러가 영어 독해에 부담을 느껴 도입) | 번역 오류·뉘앙스 손실이 판정에 영향 줄 수 있음. 단 **판정 자체는 전부 사람이 함** — 번역은 통역이지 심사가 아님 | ① 원문을 항상 병기해 수치·용어를 대조 가능하게 함 ② 번역 시 수치·모델명 원문 유지 규칙 ③ 번역 수치가 원문과 일치하는지 규칙 검사로 확인 |
| 2 | **1패스는 제목만 봄** | 제목이 불친절한 논문을 놓칠 수 있음 | **`recheck`로 누락률을 실측** (§2.4). `goldset.yaml`의 `summary.title_pass_recheck.miss_rate`가 그 수치입니다. 추정이 아니라 측정치를 쓸 수 있습니다 |
| 3 | **3일치 = 소표본** | 하루 3~7건 × 3일이면 정답이 10~20건. Hit@5의 분산이 큼 | 신뢰구간을 함께 보고, 단일 수치로 단정하지 말 것 |
| 4 | **시점 고정** | 관심사는 변함. 6개월 뒤 같은 논문을 다르게 판정할 수 있음 | 라벨링 시점을 `labeled_at` 에 기록. M6 재측정 시 골드셋 재라벨링 여부를 명시 |
| 5 | **랭커를 만들기 전에 라벨링함** | — | 이건 한계가 아니라 **강점**입니다. 랭커 출력을 보고 라벨링하면 무의식적으로 랭커에 맞춰집니다. 순서를 지켰다는 사실을 명시하세요 |

---

## 7. 완료 조건

- [ ] 3일치 후보 수집 완료 (`data/candidates/` 에 3개 파일)
- [ ] 3일치 모두 1패스 완료 — `status` 에서 보류 0건
- [ ] 3일치 모두 2패스 완료
- [ ] 3일치 모두 `recheck` 완료 — 누락률이 `goldset.yaml`에 기록됨 (§2.4)
- [ ] §3.4 · §4 · §5 를 본인 문장으로 채움
- [ ] `python -m eval.label export` 성공
- [ ] 날짜당 관련 논문이 **0건인 날이 없음** — 0건이면 Hit@5가 정의되지 않아 그날은 평가에서 빠집니다

마지막 항목이 걸리면 그날은 수집 창(48h)이 연휴에 걸렸을 가능성이 높습니다.
`--window-hours 72` 로 다시 수집하거나, 다른 날짜를 하나 더 라벨링하세요.

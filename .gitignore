# ─── 법적·약관 게이트. 지우지 마세요. tests/test_gates.py 가 검사합니다 ───
# 구직 조건 노출 방지 (기획안 §7, 델타 §D4)
data/profile.jobs.yaml
data/profile.jobs_*.yaml
# 채용공고 원문 커밋 금지 (기획안 §2, §8 M5)
data/jobs/
# arXiv 전문 저장 금지 (델타 §D1)
*.pdf
# 벤더 문서 (사람인·워크넷 배포 자료)
docs/_reference/
# ──────────────────────────────────────────────────────────────
# 상태·캐시
data/state.db
data/candidates/
data/cache/
# arXiv 레이트리미터 락 (델타 §D2 / 결정_M0 §5)
data/.arxiv_lock
.env
.env.*
# ★ 자격증명 파일 — AWS 콘솔이 내려주는 CSV 형태를 포함해 전부 차단합니다.
#   이 저장소는 public 이라 한 번 커밋되면 되돌려도 이미 유출입니다
#   (CLAUDE.md §3-2 · 사람인 약관 조항 5).
*accessKeys*.csv
*credentials*.csv
*_accessKeys.csv
credentials
*.pem
*.p12
# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
# Airflow — 로컬 메타DB·로그. DAG 정의(dags/)만 커밋한다
.airflow/
# .venv-airflow/ 는 venv 가 자체 .gitignore(*) 를 갖고 있어 별도 규칙이 필요 없다

# Hugo
site/public/
site/resources/
# macOS
.DS_Store

# S3 동기화 (src/storage/sync.py)
# 매니페스트는 이 머신의 업로드 이력. 커밋하면 다른 머신이 남의 이력을 믿고 건너뜁니다.
data/s3_manifest.json
# RADAR_MODE=fake 로 돌렸을 때의 로컬 스토어. 실제 S3가 아닙니다.
data/_fake_s3/
